import React from 'react'

function History() {
    // fetch history of the currentUser if like if the user is in any side populate there info 
    return (
        <div>
            {/* display */}

            {/* name, email, playerId , when played(date ,time) and  status ( win / lose / draw)*/}
        </div>
    )
}

export default History
