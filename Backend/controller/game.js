const Game = require('../model/Game')
const User = require('../model/user')
module.exports.getHistory = async (req, res) => {
// idea for history check any part having the same both 
// user if yes then send that history if no pair new
}

module.exports.getName = async (req, res) => {
// get user name by id 
}

module.exports.postHistory  = async (req, res) => {
// get the user id who win track that and store in the array of winner 
// game end and no id then store string drow 
// await find(player1 , player2 by id) push if id then id else drow 
}
